How far did the agent get? Did it fully implement your spec? What percentage of your acceptance criteria passed on the first try?
- The agent was able to create all of the files specified and created a mostly working version. There were no main errors from my own quick overview testing besides from being able to view stats. I think all of the acceptance criteria passed, but I should have added more acceptance criteria and edge cases to prevent other problems. It passed 7/9 acceptance criteria, but I also should have added more criteria. 

Where did you intervene? List each time you had to step in during Phase 2. Why was the intervention needed? Could a better spec have prevented it?
-  I did not intervene in Phase 2 as I thought we were not supposed to intervene (per the assignment where it stated to commit the raw unchanged parts, so I thought it would be more clear if I worked on it after Phase 3).
I do think a better spec that was more detailed regarding specific user intertactions would have yielded a better result though. 

How useful was the AI review? Did it catch real bugs? Did it miss anything important? Did it flag things that weren't actually problems?
- I think the AI review was very useful as it caught things I probably would not have noticed like feedback indexing consistency, weak pasword hashing, etc as I personally don't have much experience regarding those things. It also caught the issue with the user feedback not actually affecting later questions as well. It flagged some things I didn't really understand as well like what I mentioned in the previous sentence and some things I believe weren't actual problems like the hashing and the use of pickle. 

Spec quality → output quality: In hindsight, what would you change about your spec to get a better result from the agent?
- In hindsight, I would have added more parameters to the check at the end, and would have added more detailed instructions for the user flow with exact interactions and edge cases. I had to fix problems with questions repeating during the same quiz which I think could have been fixed by specifying that in the spec. I also fixed viewing past history ( that was just a bug), but having parameters to check if it actually worked may have prevented that issue. There were also some user flows that I discovered after testing like after selecting new account and typing in an existing username, there was no way to get back to instead selecting log in (if users accidentally chose new username) without exiting from the app itself and restarting. Thinking more deeply about cases like that and specifying those could have yielded better results. 

When would you use this workflow? Based on this experience, when do you think plan-delegate-review is better than conversational back-and-forth? When is it worse?
- I would use this workflow to create something if I already have a concrete idea of what exactly I want to do and what exactly needs to be in the actual folder or files. For this to work properly and accurately, I have to comprehend the entire project as a whole and document it for context. This method might be worse than conversational back and forth if you aren't completely sure what you are trying to create, and make changes in the core app afterwards (rendering the spec useless). It might be better for straightforward assignments or apps like this where there is core basis for what is being created rather than a more creative project like a game or a more artistic project. 